# DO NOT EDIT: This file was generated.

name: Test & Deploy
concurrency: ci-${{ github.ref }}
'on':
  pull_request: {}
  push:
    branches:
      - main
  workflow_dispatch: {}
env:
  CYPRESS_CACHE_FOLDER: node_modules/.cache/cypress
jobs:
  build:
    name: 📦 Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: actions/cache@v3
        with:
          path: |
            .angular
            libs/*/.angular
            node_modules/.cache
          key: cache
      - uses: actions/setup-node@v3
        with:
          cache: yarn
          node-version: '18'
      - name: Install
        run: yarn install --immutable
      - name: Build
        run: >-
          yarn affected --target=prerender --configuration next
          --base=deployed/marmicode-next
      - name: Notify on failure
        uses: pioug/le-slack-message@v1.0.0
        if: failure()
        with:
          JOB: ${{ toJson(job) }}
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
  lint:
    name: 🧹 Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: actions/cache@v3
        with:
          path: |
            .angular
            libs/*/.angular
            node_modules/.cache
          key: cache
      - uses: actions/setup-node@v3
        with:
          cache: yarn
          node-version: '18'
      - name: Install
        run: yarn install --immutable
      - name: Lint
        run: yarn affected --target=lint --base=deployed/marmicode-next
      - name: Notify on failure
        uses: pioug/le-slack-message@v1.0.0
        if: failure()
        with:
          JOB: ${{ toJson(job) }}
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
  test:
    name: ✅ Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v3
        with:
          cache: yarn
          node-version: '18'
      - uses: actions/cache@v3
        with:
          path: |
            .angular
            libs/*/.angular
            node_modules/.cache
          key: cache
      - name: Install
        run: yarn install --immutable
      - name: Test
        run: yarn affected --target=test --base=deployed/marmicode-next
      - name: Notify on failure
        uses: pioug/le-slack-message@v1.0.0
        if: failure()
        with:
          JOB: ${{ toJson(job) }}
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
  component-test:
    name: ✅ Component Test
    runs-on: ubuntu-latest
    env:
      PERCY_TOKEN: ${{ secrets.PERCY_TOKEN }}
      PERCY_FULL_TOKEN: ${{ secrets.PERCY_FULL_TOKEN }}
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: actions/cache@v3
        with:
          path: |
            .angular
            libs/*/.angular
            node_modules/.cache
          key: cache
      - uses: actions/setup-node@v3
        with:
          cache: yarn
          node-version: '18'
      - name: Install
        run: yarn install --immutable
      - name: All Component Tests
        if: github.ref == 'refs/heads/main'
        run: >-
          yarn percy exec --parallel -- yarn nx run-many --target=component-test
          --browser=chrome --skip-nx-cache
      - name: Affected Component Tests
        if: github.ref != 'refs/heads/main'
        run: >-
          yarn percy exec --parallel --partial -- yarn nx affected
          --target=component-test --browser=chrome
          --base=deployed/marmicode-next
      - name: Upload dist (Cypress videos / build output etc...) on failure
        uses: actions/upload-artifact@v2
        if: failure()
        with:
          name: dist
          path: dist
      - name: Notify on failure
        uses: pioug/le-slack-message@v1.0.0
        if: failure()
        with:
          JOB: ${{ toJson(job) }}
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
  e2e:
    name: ✅ End-to-End
    runs-on: ubuntu-latest
    env:
      PERCY_TOKEN: ${{ secrets.PERCY_TOKEN }}
      PERCY_FULL_TOKEN: ${{ secrets.PERCY_FULL_TOKEN }}
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: actions/cache@v3
        with:
          path: |
            .angular
            libs/*/.angular
            node_modules/.cache
          key: cache
      - uses: actions/setup-node@v3
        with:
          cache: yarn
          node-version: '18'
      - name: Install
        run: yarn install --immutable
      - name: All E2E
        if: github.ref == 'refs/heads/main'
        run: >-
          yarn percy exec --parallel -- yarn nx run-many --target=e2e
          --browser=chrome --skip-nx-cache
      - name: Affected E2E
        if: github.ref != 'refs/heads/main'
        run: >-
          yarn percy exec --parallel --partial -- yarn nx affected --target=e2e
          --browser=chrome --base=deployed/marmicode-next
      - name: Upload dist (Cypress videos / build output etc...) on failure
        uses: actions/upload-artifact@v2
        if: failure()
        with:
          name: dist
          path: dist
      - name: Notify on failure
        uses: pioug/le-slack-message@v1.0.0
        if: failure()
        with:
          JOB: ${{ toJson(job) }}
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
  percy:
    name: 🖼 Finalize Percy
    runs-on: ubuntu-latest
    env:
      PERCY_TOKEN: ${{ secrets.PERCY_TOKEN }}
      PERCY_FULL_TOKEN: ${{ secrets.PERCY_FULL_TOKEN }}
    needs:
      - component-test
      - e2e
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: actions/cache@v3
        with:
          path: |
            .angular
            libs/*/.angular
            node_modules/.cache
          key: cache
      - uses: actions/setup-node@v3
        with:
          cache: yarn
          node-version: '18'
      - name: Install
        run: yarn install --immutable
      - name: Add placeholder snapshot
        run: >-
          yarn percy exec --parallel -- yarn ts-node
          tools/percy-upload-placeholder.ts
      - name: Finalize Percy parallel build
        run: yarn percy build:finalize
      - name: Check Percy Review
        if: github.ref == 'refs/heads/main'
        run: >-
          PERCY_TOKEN=$PERCY_FULL_TOKEN yarn percy build:wait --project
          marmicode/marmicode --fail-on-changes
  deploy-marmicode-next:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    needs:
      - build
      - lint
      - test
      - component-test
      - e2e
      - percy
    name: 🚀 Marmicode Next
    environment:
      name: Marmicode Next
      url: https://marmicode-next.web.app
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: actions/cache@v3
        with:
          path: |
            .angular
            libs/*/.angular
            node_modules/.cache
          key: cache
      - uses: actions/setup-node@v3
        with:
          cache: yarn
          node-version: '18'
      - name: Install
        run: yarn install --immutable
      - name: Deploy
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
        run: yarn nx run marmicode:deploy:next
      - name: Tag deployed/marmicode-next
        run: git tag -f deployed/marmicode-next
      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: ${{ github.ref }}
          force: true
          tags: true
      - name: Upload dist (Cypress videos / build output etc...) on failure
        uses: actions/upload-artifact@v2
        if: failure()
        with:
          name: dist
          path: dist
      - name: Notify on failure
        uses: pioug/le-slack-message@v1.0.0
        if: failure()
        with:
          JOB: ${{ toJson(job) }}
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
  deploy-marmicode:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    needs:
      - build
      - lint
      - test
      - component-test
      - e2e
      - percy
    name: 🚀 Marmicode
    environment:
      name: Marmicode
      url: https://marmicode.io
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: actions/cache@v3
        with:
          path: |
            .angular
            libs/*/.angular
            node_modules/.cache
          key: cache
      - uses: actions/setup-node@v3
        with:
          cache: yarn
          node-version: '18'
      - name: Install
        run: yarn install --immutable
      - name: Deploy
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
        run: yarn nx run marmicode:deploy:production
      - name: Tag deployed/marmicode
        run: git tag -f deployed/marmicode
      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: ${{ github.ref }}
          force: true
          tags: true
      - name: Upload dist (Cypress videos / build output etc...) on failure
        uses: actions/upload-artifact@v2
        if: failure()
        with:
          name: dist
          path: dist
      - name: Notify on failure
        uses: pioug/le-slack-message@v1.0.0
        if: failure()
        with:
          JOB: ${{ toJson(job) }}
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
