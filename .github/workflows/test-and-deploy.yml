# DO NOT EDIT: This file was generated.

name: Test & Deploy
concurrency: ci-${{ github.ref }}
'on':
  pull_request: {}
  push:
    branches:
      - main
  workflow_dispatch: {}
env:
  CYPRESS_CACHE_FOLDER: node_modules/.cache/cypress
jobs:
  build:
    name: 📦 Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: actions/cache@v3
        with:
          path: |
            .angular
            libs/*/.angular
            node_modules/.cache
          key: cache
      - uses: actions/setup-node@v3
        with:
          cache: yarn
          node-version: '18'
      - name: Install
        run: yarn install --immutable
      - name: Build
        run: >-
          yarn affected --target=prerender --configuration next
          --base=deployed/marmicode-next
  lint:
    name: 🧹 Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: actions/cache@v3
        with:
          path: |
            .angular
            libs/*/.angular
            node_modules/.cache
          key: cache
      - uses: actions/setup-node@v3
        with:
          cache: yarn
          node-version: '18'
      - name: Install
        run: yarn install --immutable
      - name: Lint
        run: yarn affected --target=lint --base=deployed/marmicode-next
  test:
    name: ✅ Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v3
        with:
          cache: yarn
          node-version: '18'
      - uses: actions/cache@v3
        with:
          path: |
            .angular
            libs/*/.angular
            node_modules/.cache
          key: cache
      - name: Install
        run: yarn install --immutable
      - name: Test
        run: yarn affected --target=test --base=deployed/marmicode-next
  ct-and-e2e:
    name: ✅ Component Test & End-to-End
    runs-on: ubuntu-latest
    env:
      PERCY_TOKEN: ${{ secrets.PERCY_TOKEN }}
      PERCY_FULL_TOKEN: ${{ secrets.PERCY_FULL_TOKEN }}
      PERCY_PARALLEL_TOTAL: 4
      NX_CLOUD_DISTRIBUTED_EXECUTION: true
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: actions/cache@v3
        with:
          path: |
            .angular
            libs/*/.angular
            node_modules/.cache
          key: cache
      - uses: actions/setup-node@v3
        with:
          cache: yarn
          node-version: '18'
      - name: Install
        run: yarn install --immutable
      - run: yarn nx-cloud start-ci-run
      - name: All Component Tests & E2E
        if: github.ref == 'refs/heads/main'
        run: |
          yarn nx run-many --target=component-test --browser=chrome \
          & yarn nx run-many --target=e2e --browser=chrome
      - name: Affected Component Tests & E2E
        if: github.ref != 'refs/heads/main'
        run: >
          yarn nx affected --target=component-test --browser=chrome
          --base=deployed/marmicode-next \

          & yarn nx affected --target=e2e --browser=chrome
          --base=deployed/marmicode-next
      - run: yarn nx-cloud stop-all-agents
      - name: Add placeholder snapshot
        run: >-
          yarn percy exec --parallel -- yarn ts-node
          tools/percy-upload-placeholder.ts
      - name: Check Percy Review
        if: github.ref == 'refs/heads/main'
        run: >-
          PERCY_TOKEN=$PERCY_FULL_TOKEN yarn percy build:wait --project
          marmicode/marmicode --fail-on-changes
      - name: Upload dist (Cypress videos / build output etc...) on failure
        uses: actions/upload-artifact@v2
        if: failure()
        with:
          name: dist
          path: dist
  nx-agents:
    name: 🤖 Nx Agents
    runs-on: ubuntu-latest
    env:
      PERCY_TOKEN: ${{ secrets.PERCY_TOKEN }}
      PERCY_FULL_TOKEN: ${{ secrets.PERCY_FULL_TOKEN }}
      PERCY_PARALLEL_TOTAL: 4
    strategy:
      matrix:
        agent:
          - 1
          - 2
          - 3
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: actions/cache@v3
        with:
          path: |
            .angular
            libs/*/.angular
            node_modules/.cache
          key: cache
      - uses: actions/setup-node@v3
        with:
          cache: yarn
          node-version: '18'
      - name: Install
        run: yarn install --immutable
      - if: github.ref == 'refs/heads/main'
        run: yarn percy exec --parallel -- yarn nx-cloud start-agent
      - if: github.ref != 'refs/heads/main'
        run: yarn percy exec --parallel --partial -- yarn nx-cloud start-agent
      - name: Upload dist (Cypress videos / build output etc...) on failure
        uses: actions/upload-artifact@v2
        if: failure()
        with:
          name: dist
          path: dist
  deploy-marmicode-next:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    needs:
      - build
      - lint
      - test
      - ct-and-e2e
    name: 🚀 Marmicode Next
    environment:
      name: Marmicode Next
      url: https://marmicode-next.web.app
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: actions/cache@v3
        with:
          path: |
            .angular
            libs/*/.angular
            node_modules/.cache
          key: cache
      - uses: actions/setup-node@v3
        with:
          cache: yarn
          node-version: '18'
      - name: Install
        run: yarn install --immutable
      - name: Deploy
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
        run: yarn nx run marmicode:deploy:next
      - name: Tag deployed/marmicode-next
        run: git tag -f deployed/marmicode-next
      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: ${{ github.ref }}
          force: true
          tags: true
      - name: Upload dist (Cypress videos / build output etc...) on failure
        uses: actions/upload-artifact@v2
        if: failure()
        with:
          name: dist
          path: dist
  deploy-marmicode:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    needs:
      - build
      - lint
      - test
      - ct-and-e2e
    name: 🚀 Marmicode
    environment:
      name: Marmicode
      url: https://marmicode.io
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: actions/cache@v3
        with:
          path: |
            .angular
            libs/*/.angular
            node_modules/.cache
          key: cache
      - uses: actions/setup-node@v3
        with:
          cache: yarn
          node-version: '18'
      - name: Install
        run: yarn install --immutable
      - name: Deploy
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
        run: yarn nx run marmicode:deploy:production
      - name: Tag deployed/marmicode
        run: git tag -f deployed/marmicode
      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: ${{ github.ref }}
          force: true
          tags: true
      - name: Upload dist (Cypress videos / build output etc...) on failure
        uses: actions/upload-artifact@v2
        if: failure()
        with:
          name: dist
          path: dist
